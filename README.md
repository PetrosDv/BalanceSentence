# BalanceSentence

This project takes a sentence from console and checks if the brackets in it are balanced.
e.g.
{{)(}} is not balanced
({)} is not balanced

[({})] is balanced
{}([]) is balanced
{()}[[{}]] is balanced
